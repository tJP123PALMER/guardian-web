Guardian Operations v2.4.2 — Standby Selection Fix

Fix:
- Manual Standby Move appliance selection now persists through live refreshes.
- Destination station selection persists through live refreshes.
- Control note persists while editing.
- Selections only clear after a standby move is successfully sent.
- USE SUGGESTION now fills the manual form for operator confirmation.
- All v2.4.1 Book On/Book Off fixes and standby dispatch functionality retained.

Install the web package as normal. No FiveM replacement is required if v2.4.1
FiveM files are already installed, because this patch is Control UI only.
